# S1 参数变更传播案例

本目录只包含虚构资料，用于验证已确认参数发生变化时，工作包能先列出完整影响范围，而不是只修改当前看到的一处。

## 测试任务

读取 `baseline.md` 和 `change-request.md`。先冻结输入并核对变更授权，只输出变更影响计划和更新后的决定/TBD 状态，不直接改写基线正文。

## 最低通过条件

影响计划至少覆盖：项目控制表、方案摘要、总体设计、样本量与统计、随机分组、受试者流程、表格/图示、相互引用和版本记录；同时标明样本量依据仍需复核，不得因为总例数和分配数已确认就声称统计依据充分。
