# preview.5 必读规范接入验证

日期：2026-09-07；环境：macOS、Python 3.13.1。

- 87/87 回归通过：原有 80 项 + 新增 7 项规范读取支持测试。新增测试覆盖正确文件但不冒称已读、缺失、内容改变、显式错误路径不回退、本地重复登记、已有不同内容不覆盖、分段文本与范围越界。
- 本轮主任务实际分段完整读取指定原文 1–1,520 行至 EOF；原文与登记的本机副本均匹配 writing-standard.lock.json 的固定 SHA-256，正文版本 v2.3 / 2026-09-02。
- 本机副本存放在 Skill 目录之外的规范目录，不随公开发行。原文未修改，未把其中内部案例提交到仓库。
- SKILL 总入口、撰写模式、审核模式、项目 AGENTS 模板和控制表都链接或记录同一必读要求。相对文档链接与 Skill 结构校验通过。
- 安装报告增加规范可用性；文件指纹匹配仍标记 agent_read_complete=not_attested。需要执行任务实际全文阅读后记录，不由脚本自动填写完成。

本轮验证证明的是文件定位、校验、显示、保存和工作流规则接入。没有声称技术上能保证模型每轮必然遵从，也没有重新运行真实临床撰写/审核、Word 渲染或 Windows/Linux 实机检查。规范中各项方法的具体实现覆盖仍须按项目记录，不能因增加必读规则就宣称 L1 全部检查已经实现。
